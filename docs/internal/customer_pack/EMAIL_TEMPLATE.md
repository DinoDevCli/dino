# Customer email — Early Access Team Key

Canonical sendable body: [`EMAIL.txt`](EMAIL.txt)

`./scripts/issue-early-access.sh` fills `{TEAM}` `{DAYS}` `{NAME}` `{KEY}` `{VERSION}` and writes `EMAIL.txt` into the customer ZIP.

Early Access is open — your team can start immediately.

**From:** dinodevcli@gmail.com  
**Subject:** `Dino Early Access — Proof Pack Team Key ({TEAM})`
