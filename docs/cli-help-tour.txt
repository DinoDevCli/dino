=== Dino CLI help tour (v1.0 layout) ===
Generated: 2026-08-25T18:58:22+02:00
Version: 0.3.2


################################################################################
# dino --help
################################################################################

usage: dino [-h] [--version] [--json] [--dev] <command> ...

Deterministic Proof for Python Decision Pipelines

Core Workflow:
  run           Alias for `proof run`
  proof         Full proof chain
  scan          Grammar + leakage scan

Pipeline Operations:
  capsule       Capsule run/replay/doctor
  bundle        Bundle create/replay/verify/diff/archive/dedup
  map           Analyze/verify/plan/drift
  verify        Attestation + drift verification
  flight        Canary artifact summary

System & Packs:
  packs         Show active packs
  status        Engine status
  upgrade       Apply team key
  version       Show version

options:
  -h, --help            show this help message and exit
  --version             show program's version number and exit
  --json                Emit JSON envelopes (also accepted before the domain)
  --dev                 Developer Mode: relax EMPTY_SCAN_ROOTS (not for
                        production proofs)

Meta: dino version | packs | status | upgrade --pack proof --key KEY |
issue-key --team NAME [--days N] | init-license.

---
Early Access (Proof Pack)
  CI compare gate · S3/HTTP backends · engine contract stability · team mode
  These features are not part of the open-source scan engine.

  Details & instructions:
    https://github.com/DinoDevCli/dino#early-access
    Contact: dinodevcli@gmail.com

################################################################################
# dino --version
################################################################################

dino 0.3.2

################################################################################
# dino version
################################################################################

0.3.2

################################################################################
# dino packs
################################################################################

Dino packs (free + proof)

[*] free       free   forever
    Free forever — grammar smoke + causal leakage scan for research pipelines.
    domains: scan

[*] proof      ea     Early Access · free Team Key
    Proof Pack (Early Access) — capsule, map, bundle, flight, verify, proof chain, export, and index. Request a free Team Key.
    domains: capsule, map, bundle, flight, verify, proof

Unlock:  dino upgrade --pack proof --key YOUR_TEAM_KEY
See also: dino --help  (Early Access / Proof Pack)
Early Access: dinodevcli@gmail.com

################################################################################
# dino status
################################################################################

dino 0.3.2
Active packs: free, proof
  [ON ] bundle
  [ON ] capsule
  [ON ] flight
  [ON ] map
  [ON ] proof
  [ON ] scan
  [ON ] verify

################################################################################
# dino upgrade
################################################################################

Usage: dino upgrade --pack proof|free --key KEY

################################################################################
# dino run --help
################################################################################

usage: dino run [-h] [--output-dir PATH] [--command ARGV [ARGV ...]]
                [--repo PATH] [--scan [PATH ...]] [--stdin TEXT]
                [--export DEST] [--pipeline NAME] [--group NAME] [--tag TAG]
                ...

positional arguments:
  CMD                   Command after -- (alternative to --command)

options:
  -h, --help            show this help message and exit
  --output-dir PATH     Output directory for the sealed proof bundle
  --command ARGV [ARGV ...]
                        Argv to seal, e.g. echo ok (or one string: "echo ok")
  --repo PATH           Optional repository root for map verify
  --scan [PATH ...]     Optional paths for leakage scan
  --stdin TEXT          Optional stdin text sealed into the capsule
  --export DEST         Upload sealed proof: path | http(s)://… |
                        s3://bucket/prefix
  --pipeline NAME       Pipeline label for proof_index.json
  --group NAME          Group label for proof_index.json
  --tag TAG             Tag for proof_index.json (repeatable)

---
Early Access (Proof Pack)
  CI compare gate · S3/HTTP backends · engine contract stability · team mode
  These features are not part of the open-source scan engine.

  Details & instructions:
    https://github.com/DinoDevCli/dino#early-access
    Contact: dinodevcli@gmail.com

################################################################################
# dino scan --help
################################################################################

usage: dino scan [-h] {grammar,leakage} ...

positional arguments:
  {grammar,leakage}
    grammar          Expression grammar smoke test
    leakage          Static leakage scan

options:
  -h, --help         show this help message and exit

################################################################################
# dino scan leakage --help
################################################################################

usage: dino scan leakage [-h] PATH [PATH ...]

positional arguments:
  PATH        Paths to scan for causal leakage

options:
  -h, --help  show this help message and exit

################################################################################
# dino bundle --help
################################################################################

usage: dino bundle [-h] {create,replay,verify,diff,archive,dedup} ...

positional arguments:
  {create,replay,verify,diff,archive,dedup}
    create              Build complete run bundle
    replay              Compare current dump against replay baseline
    verify              Alias of replay — official regression gate (exit 1 on
                        fail)
    diff                Same comparison as verify, always exit 0 (review)
    archive             Initialize archive store
    dedup               Initialize dedup store

options:
  -h, --help            show this help message and exit

################################################################################
# dino bundle create --help
################################################################################

usage: dino bundle create [-h] [--repo-root ROOT] RUNDATA_PATH OUTPUT_PATH

positional arguments:
  RUNDATA_PATH      Path to runtime execution payload (JSON)
  OUTPUT_PATH       Output path for generated bundle JSON

options:
  -h, --help        show this help message and exit
  --repo-root ROOT  Optional repository root for map verify

################################################################################
# dino bundle replay --help
################################################################################

usage: dino bundle replay [-h] --baseline PATH --current PATH [--target-id ID]

options:
  -h, --help       show this help message and exit
  --baseline PATH  Baseline JSON or proof.json to compare against
  --current PATH   Current dump or baseline-shaped JSON
  --target-id ID   Target identifier when building baseline from a dump

################################################################################
# dino flight summary --help
################################################################################

usage: dino flight summary [-h] --artifacts-dir PATH --output PATH

options:
  -h, --help            show this help message and exit
  --artifacts-dir PATH  Directory containing canary artifacts
  --output PATH         Output path for summary JSON

################################################################################
# dino verify drift --help
################################################################################

usage: dino verify drift [-h] --distance N [--tau N] [--graph-truth PATH]

options:
  -h, --help          show this help message and exit
  --distance N        Drift distance metric
  --tau N             Drift tolerance threshold
  --graph-truth PATH  Ground-truth import graph for drift classification

################################################################################
# dino map drift --help
################################################################################

usage: dino map drift [-h] --baseline PATH [--tau N] PATH

positional arguments:
  PATH             Current graph path (file or directory)

options:
  -h, --help       show this help message and exit
  --baseline PATH  Baseline graph path to compare against
  --tau N          Drift tolerance threshold

################################################################################
# dino capsule run --help
################################################################################

usage: dino capsule run [-h] [--output-dir PATH] [--command ARGV [ARGV ...]]

options:
  -h, --help            show this help message and exit
  --output-dir PATH     Output directory for capsule artifacts
  --command ARGV [ARGV ...]
                        Argv to seal, e.g. echo ok (or one string: "echo ok")

################################################################################
# dino proof --help
################################################################################

usage: dino proof [-h] {run,verify,export,index,doctor} ...

positional arguments:
  {run,verify,export,index,doctor}
    run                 Seal command + optional scan/map into proof.json
    verify              Re-verify a proof.json bundle
    export              Upload an existing proof directory
    index               Proof index manifest (proof_index.json)
    doctor              Proof-stack health checks

options:
  -h, --help            show this help message and exit

################################################################################
# dino proof run --help
################################################################################

usage: dino proof run [-h] [--output-dir PATH] [--command ARGV [ARGV ...]]
                      [--repo PATH] [--scan [PATH ...]] [--stdin TEXT]
                      [--export DEST] [--pipeline NAME] [--group NAME]
                      [--tag TAG]
                      ...

positional arguments:
  CMD                   Command after -- (alternative to --command)

options:
  -h, --help            show this help message and exit
  --output-dir PATH     Output directory for the sealed proof bundle
  --command ARGV [ARGV ...]
                        Argv to seal, e.g. echo ok (or one string: "echo ok")
  --repo PATH           Optional repository root for map verify
  --scan [PATH ...]     Optional paths for leakage scan
  --stdin TEXT          Optional stdin text sealed into the capsule
  --export DEST         Upload sealed proof: path | http(s)://… |
                        s3://bucket/prefix
  --pipeline NAME       Pipeline label for proof_index.json
  --group NAME          Group label for proof_index.json
  --tag TAG             Tag for proof_index.json (repeatable)

---
Early Access (Proof Pack)
  CI compare gate · S3/HTTP backends · engine contract stability · team mode
  These features are not part of the open-source scan engine.

  Details & instructions:
    https://github.com/DinoDevCli/dino#early-access
    Contact: dinodevcli@gmail.com

################################################################################
# dino proof verify --help
################################################################################

usage: dino proof verify [-h] --proof PATH

options:
  -h, --help    show this help message and exit
  --proof PATH  Path to proof.json

################################################################################
# dino proof export --help
################################################################################

usage: dino proof export [-h] [--proof-dir PATH] [--proof PATH] --to DEST
                         [--pipeline NAME] [--group NAME] [--tag TAG]

options:
  -h, --help        show this help message and exit
  --proof-dir PATH  Directory containing proof.json (default: parent of
                    --proof)
  --proof PATH      Path to proof.json (alternative to --proof-dir)
  --to DEST         Destination: path | http(s)://… | s3://bucket/prefix
  --pipeline NAME   Pipeline label for proof_index.json
  --group NAME      Group label for proof_index.json
  --tag TAG         Tag for proof_index.json (repeatable)

################################################################################
# dino proof doctor --help
################################################################################

usage: dino proof doctor [-h] [--output-dir PATH]

options:
  -h, --help         show this help message and exit
  --output-dir PATH  Optional output directory for doctor artifacts

################################################################################
# dino proof index --help
################################################################################

usage: dino proof index [-h] {show,rebuild,compare,metrics,layout} ...

positional arguments:
  {show,rebuild,compare,metrics,layout}
    show                Print proof_index.json
    rebuild             Rebuild index from archive subfolders
    compare             Compare two proofs by hash/prefix
    metrics             Aggregate health summary JSON
    layout              Refresh pipelines/groups/tags browse links

options:
  -h, --help            show this help message and exit

################################################################################
# dino proof index compare --help
################################################################################

usage: dino proof index compare [-h] PATH HASH HASH

positional arguments:
  PATH        Archive directory
  HASH        First proof hash (or prefix / path)
  HASH        Second proof hash (or prefix / path)

options:
  -h, --help  show this help message and exit

---
Early Access (Proof Pack)
  CI compare gate · S3/HTTP backends · engine contract stability · team mode
  These features are not part of the open-source scan engine.

  Details & instructions:
    https://github.com/DinoDevCli/dino#early-access
    Contact: dinodevcli@gmail.com

################################################################################
# dino proof index show --help
################################################################################

usage: dino proof index show [-h] PATH

positional arguments:
  PATH        Archive directory containing proof_index.json

options:
  -h, --help  show this help message and exit

################################################################################
# dino proof index metrics --help
################################################################################

usage: dino proof index metrics [-h] PATH

positional arguments:
  PATH        Archive directory

options:
  -h, --help  show this help message and exit
